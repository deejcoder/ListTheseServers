import React from 'react';


/**
 * A dialog to show the downtime of a server
 */
class ServerActivity extends React.Component {

    constructor() {
        this.state = {
            server_id = null,
            activity = [],
        }
    }

    render() {
        return (
            <React.Fragment>

            </React.Fragment>
        );
    }
}

export default ServerActivity;